# CLAUDE.md (src/)

Stricter rules apply here than the rest of the repo, once src/ has real code:

- Public functions/classes get docstrings. Names prefixed with `_` are exempt.
- Every new module needs at least one corresponding test under `src/tests/`
  before it's considered done — see `skills/test-writing` for what counts.
